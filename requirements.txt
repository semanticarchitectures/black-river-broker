# ── Core agent framework ──────────────────────────────────────────────────
langchain>=0.3.0
langchain-anthropic>=0.3.0
langchain-core>=0.3.0
langgraph>=0.2.0

# ── Blockchain ────────────────────────────────────────────────────────────
web3>=7.0.0
pytempo>=0.1.0         # Tempo Transaction builder (pip install pytempo)

# ── Data models ───────────────────────────────────────────────────────────
pydantic>=2.7.0

# ── Utilities ─────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
rich>=13.0.0          # pretty terminal output for the audit trail viewer

# ── Phase 2: MPP broker server ────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0          # async HTTP client for mpp_consumer + Phase 3 producers
pympp[tempo]>=0.1.0    # Machine Payments Protocol SDK (pip install 'pympp[tempo]')

# ── Phase 3: real partner API adapters ────────────────────────────────────
tenacity>=8.3.0        # retry / exponential back-off for HTTP producers

# ── Development / testing ─────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
respx>=0.21.0          # httpx request mocking for Phase 3 adapter tests
